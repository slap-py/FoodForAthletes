{
  "name": "dayplate-catalog-service",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node src/local.js",
    "test": "node --test",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  }
}
